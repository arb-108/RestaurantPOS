<Window x:Class="RestaurantPOS.WPF.Views.UnPaidBillWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Un-Paid Bill - Reason Required"
        Height="300" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#DAEDF4"
        FontFamily="Segoe UI">

    <Grid Margin="24,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Warning icon + message -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,16">
            <TextBlock Text="&#xE7BA;" FontFamily="Segoe MDL2 Assets"
                       FontSize="28" Foreground="#E65100"
                       VerticalAlignment="Center" Margin="0,0,12,0"/>
            <TextBlock Text="This order will be marked as Un-Paid.&#x0a;Please provide a reason (required)."
                       FontSize="14" Foreground="#333"
                       TextWrapping="Wrap" VerticalAlignment="Center"/>
        </StackPanel>

        <!-- Label -->
        <TextBlock Grid.Row="1" FontSize="15" FontWeight="SemiBold" Margin="0,0,0,6">
            <Run Text="Reason:"/><Run Text="*" Foreground="Red"/>
        </TextBlock>

        <!-- Reason TextBox -->
        <TextBox x:Name="TxtReason" Grid.Row="2" FontSize="14" Padding="8,6"
                 AcceptsReturn="True" TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 VerticalContentAlignment="Top"
                 BorderBrush="#666" BorderThickness="1"
                 Margin="0,0,0,16"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="BtnCancel" Content="Cancel" Click="Cancel_Click"
                    Width="110" Height="40" FontSize="15" FontWeight="Bold"
                    Foreground="White" Cursor="Hand" Margin="0,0,12,0">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#9E9E9E" CornerRadius="4" Padding="8,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <Button x:Name="BtnConfirm" Content="Confirm" Click="Confirm_Click"
                    Width="110" Height="40" FontSize="15" FontWeight="Bold"
                    Foreground="White" Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="#E53935" CornerRadius="4" Padding="8,4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.85"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Grid>
</Window>
