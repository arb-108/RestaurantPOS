<Window x:Class="RestaurantPOS.WPF.Views.BackupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Backup &amp; Restore"
        Width="620" Height="540"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        ResizeMode="NoResize"
        AllowsTransparency="True"
        Background="Transparent"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <Style x:Key="ActionBtn" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}" CornerRadius="4" Padding="12,8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.88"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="Bd" Property="Opacity" Value="0.45"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="SecondaryBtn" TargetType="Button">
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="White" BorderBrush="#9CA3AF" BorderThickness="1" CornerRadius="4" Padding="12,6">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#F3F4F6"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleBtn" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontFamily" Value="Segoe MDL2 Assets"/>
            <Setter Property="FontSize" Value="10"/>
            <Setter Property="Width" Value="44"/>
            <Setter Property="Height" Value="36"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#3D6F9E"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border BorderBrush="#4682B4" BorderThickness="2" Background="White" CornerRadius="2">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="36"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Title bar -->
            <Border Grid.Row="0" Background="#4682B4" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown">
                <Grid>
                    <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Margin="12,0">
                        <TextBlock Text="&#xEDB3;" FontFamily="Segoe MDL2 Assets" Foreground="White" FontSize="14" VerticalAlignment="Center" Margin="0,0,8,0"/>
                        <TextBlock Text="Database Backup &amp; Restore" Foreground="White" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                    <Button HorizontalAlignment="Right" Style="{StaticResource TitleBtn}" Content="&#xE8BB;" Click="Close_Click"/>
                </Grid>
            </Border>

            <!-- Content -->
            <Grid Grid.Row="1" Margin="20">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Info cards -->
                <Grid Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Border Grid.Column="0" Background="#EFF6FF" CornerRadius="4" Padding="12" Margin="0,0,6,0">
                        <StackPanel>
                            <TextBlock Text="Database Size" FontSize="11" Foreground="#6B7280"/>
                            <TextBlock x:Name="TxtDbSize" Text="-" FontSize="16" FontWeight="Bold" Foreground="#1E40AF"/>
                        </StackPanel>
                    </Border>

                    <Border Grid.Column="1" Background="#F0FDF4" CornerRadius="4" Padding="12" Margin="6,0,0,0">
                        <StackPanel>
                            <TextBlock Text="Last Backup" FontSize="11" Foreground="#6B7280"/>
                            <TextBlock x:Name="TxtLastBackup" Text="Never" FontSize="16" FontWeight="Bold" Foreground="#059669" TextTrimming="CharacterEllipsis"/>
                        </StackPanel>
                    </Border>
                </Grid>

                <!-- Backup folder -->
                <Border Grid.Row="1" Background="#FAF5FF" CornerRadius="4" Padding="12" Margin="0,10,0,0">
                    <StackPanel>
                        <TextBlock Text="Backup Folder" FontSize="11" Foreground="#6B7280"/>
                        <TextBlock x:Name="TxtBackupPath" Text="-" FontSize="11" Foreground="#7C3AED" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <!-- Action buttons -->
                <UniformGrid Grid.Row="2" Columns="3" Margin="0,14,0,0">
                    <Button x:Name="BtnBackup" Style="{StaticResource ActionBtn}" Background="#059669" Height="56" Margin="0,0,4,0" Click="Backup_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE74E;" FontFamily="Segoe MDL2 Assets" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Backup Now" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnImport" Style="{StaticResource ActionBtn}" Background="#2563EB" Height="56" Margin="4,0,4,0" Click="Import_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE8B5;" FontFamily="Segoe MDL2 Assets" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Import Backup" VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                    <Button x:Name="BtnExport" Style="{StaticResource ActionBtn}" Background="#7C3AED" Height="56" Margin="4,0,0,0" Click="Export_Click">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xEDE1;" FontFamily="Segoe MDL2 Assets" FontSize="16" VerticalAlignment="Center" Margin="0,0,8,0"/>
                            <TextBlock Text="Export As..." VerticalAlignment="Center"/>
                        </StackPanel>
                    </Button>
                </UniformGrid>

                <!-- Progress + status -->
                <Border Grid.Row="3" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="4" Padding="14" Margin="0,14,0,0">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" x:Name="TxtStatusTitle" Text="Ready" FontSize="13" FontWeight="SemiBold" Foreground="#111827"/>
                        <TextBlock Grid.Row="1" x:Name="TxtStatusMessage"
                                   Text="Click Backup Now to create a backup, Import to load an existing .bak file, or Export to save a copy elsewhere."
                                   FontSize="11" Foreground="#6B7280" TextWrapping="Wrap" Margin="0,4,0,0" MinHeight="32"/>
                        <ProgressBar Grid.Row="2" x:Name="ProgressBar" Height="6" Minimum="0" Maximum="100"
                                     Margin="0,8,0,0" IsIndeterminate="False" Visibility="Collapsed"
                                     Background="#E5E7EB" Foreground="#4682B4" BorderThickness="0"/>
                    </Grid>
                </Border>

                <!-- Instructions -->
                <Border Grid.Row="4" Background="#FFFBEB" BorderBrush="#FCD34D" BorderThickness="1" CornerRadius="4" Padding="12" Margin="0,14,0,0" VerticalAlignment="Top">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,6">
                            <TextBlock Text="&#xE946;" FontFamily="Segoe MDL2 Assets" FontSize="13" Foreground="#92400E" VerticalAlignment="Center" Margin="0,0,6,0"/>
                            <TextBlock Text="Notes" FontSize="12" FontWeight="Bold" Foreground="#92400E"/>
                        </StackPanel>
                        <TextBlock FontSize="11" Foreground="#78350F" TextWrapping="Wrap">
                            <Run Text="• Backup Now: creates a .bak, keeps the 7 most recent, and copies the latest to any connected USB drive."/>
                            <LineBreak/>
                            <Run Text="• Import Backup: smart-merges a .bak — only rows missing from the live database are inserted. Existing rows are never overwritten."/>
                            <LineBreak/>
                            <Run Text="• Export: writes a .bak to a location you choose (e.g. USB or network share)."/>
                        </TextBlock>
                    </StackPanel>
                </Border>
            </Grid>

            <!-- Footer -->
            <Border Grid.Row="2" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="0,1,0,0" Padding="16,10">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Style="{StaticResource SecondaryBtn}" Content="Open Backup Folder" Width="160" Height="32" Margin="0,0,8,0" Click="OpenFolder_Click"/>
                    <Button Style="{StaticResource ActionBtn}" Background="#6B7280" Content="Close" Width="90" Height="32" Click="Close_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
